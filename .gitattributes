# Golden files and generated Terraform are byte-compared in tests; keep LF on
# every platform (Windows runners default to autocrlf).
* text=auto eol=lf
*.ps1 text eol=crlf
# Empty patch context lines require a space marker.
examples/minilake/compatibility.patch whitespace=-blank-at-eol
